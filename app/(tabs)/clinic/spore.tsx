/* TEMP CODES */

import { StyleSheet, Text, View } from 'react-native';

export default function SporeScreen() {
  return (
    <View style={styles.container}>
      <Text>Spore Test Screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
